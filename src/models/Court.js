export default class Court {
    constructor(imageUrl, name, vendorId, typeId, courtSizeId) {
      this.imageUrl = imageUrl;
      this.name = name;
      this.vendorId = vendorId;
      this.typeId = typeId;
      this.courtSizeId = courtSizeId;
    }
  }