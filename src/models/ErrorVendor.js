export default class ErrorVendor {
  constructor(Name, Email, OpenTime, c, PhoneNumber, Address) {
    this.Name = Name;
    this.Email = Email;
    this.OpenTime = OpenTime;
    this.OpenTime = OpenTime;
    this.PhoneNumber = PhoneNumber;
    this.Address = Address;
  }
}
